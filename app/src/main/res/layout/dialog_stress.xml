<?xml version="1.0" encoding="utf-8"?>
<!-- Диалог добавления/редактирования записи словаря ударений: слово, чипы по гласным
     (перестраиваются на каждое изменение текста — см. StressFragment.showDialog) и две
     кнопки прослушивания — «как модель» (без словаря) и «с ударением» (как выбрано). -->
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">
<LinearLayout
    android:orientation="vertical"
    android:paddingHorizontal="12dp"
    android:paddingTop="8dp"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <com.google.android.material.textfield.TextInputLayout
        style="@style/Widget.Material3.TextInputLayout.OutlinedBox"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/stress_word">
        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/word"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:inputType="text|textNoSuggestions" />
    </com.google.android.material.textfield.TextInputLayout>

    <!-- Подпись чипов: без неё TalkBack читает «моло́ко, флажок» и не объясняет, что выбирается. -->
    <TextView
        android:id="@+id/chipsLabel"
        style="@style/SectionHint"
        android:layout_marginTop="12dp"
        android:layout_marginBottom="0dp"
        android:text="@string/stress_vowel" />

    <com.google.android.material.chip.ChipGroup
        android:id="@+id/chips"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        app:singleSelection="true"
        app:selectionRequired="true" />

    <!-- match_parent у кнопок: длинная подпись переносится на две строки, короткая нет —
         без этого кнопки разной высоты и не в линию. -->
    <LinearLayout
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp">
        <Button
            android:id="@+id/listenModel"
            style="@style/Widget.Material3.Button.TonalButton"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="match_parent"
            android:layout_marginEnd="6dp"
            android:text="@string/listen_as_model" />
        <Button
            android:id="@+id/listenStressed"
            style="@style/Widget.Material3.Button.TonalButton"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="match_parent"
            android:layout_marginStart="6dp"
            android:text="@string/listen_stressed" />
    </LinearLayout>
</LinearLayout>
</ScrollView>
