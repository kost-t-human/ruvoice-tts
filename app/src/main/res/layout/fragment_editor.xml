<?xml version="1.0" encoding="utf-8"?>
<!-- Общая страница для словаря ударений и замен: подсказка по синтаксису, пример,
     редактор на всю оставшуюся высоту (текст прокручивается внутри поля). -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:padding="16dp"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/hint"
        style="@style/SectionHint"
        android:layout_marginBottom="8dp" />

    <TextView
        android:id="@+id/example"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="12dp"
        android:padding="10dp"
        android:background="@drawable/bg_example"
        android:fontFamily="monospace"
        android:textAppearance="?attr/textAppearanceBodySmall"
        android:textColor="?attr/colorOnSurfaceVariant" />

    <com.google.android.material.textfield.TextInputLayout
        android:id="@+id/editorLayout"
        style="@style/Widget.Material3.TextInputLayout.OutlinedBox"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        app:hintEnabled="false">
        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/editor"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="top|start"
            android:fontFamily="monospace"
            android:textSize="14sp"
            android:inputType="textMultiLine|textNoSuggestions"
            android:scrollbars="vertical" />
    </com.google.android.material.textfield.TextInputLayout>
</LinearLayout>
